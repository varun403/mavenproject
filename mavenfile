pipeline{
    agent any
    tools {
        maven 'Apache Maven 3.6.3 ' 
    }
    stages{
        stage('checkout'){
            steps{
                checkout scm
            }
        }
        stage('Build'){
            steps{
                sh "echo $PATH"
                sh "mvn clean install"
            }
        }
    }
}
